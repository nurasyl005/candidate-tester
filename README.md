## 🚀 Быстрый старт

### Предварительные требования

- **Node.js** версии 16+
- **PostgreSQL** 12+
- **Git**

### 1️⃣ Клонирование репозитория

```bash
git clone https://github.com/<your_username>/candidate-tester.git
cd candidate-tester
```

### 2️⃣ Установка зависимостей

```bash
npm install
```

### 3️⃣ Настройка базы данных

Создайте базу данных PostgreSQL:

```sql
CREATE DATABASE candidate_test;
```

Затем создайте файл `.env` в корне проекта:

```env
# База данных
DB_HOST=localhost
DB_PORT=5432
DB_NAME=candidate_test
DB_USER=postgres
DB_PASSWORD=your_password

# Сервер
PORT=3000
NODE_ENV=development
```

Инициализируйте таблицы и добавьте тестовые данные:

```bash
psql -U postgres -d candidate_test -f backend/db/init.sql
```

### 4️⃣ Запуск приложения

```bash
npm start
```

Откройте приложение по адресу 👉 **http://localhost:3000**

---

## 📁 Структура проекта

```
candidate-tester/
├── backend/                 # Логика backend
│   ├── classes/             # Модели данных и логика CRUD
│   ├── controllers/         # Контроллеры Express API
│   ├── db/                  # Подключение к PostgreSQL + init.sql
│   ├── utils/               # Вспомогательные утилиты
│   ├── router.js            # Маршрутизация API
│   └── server.js            # Точка входа приложения
├── frontend/                # Файлы frontend
│   ├── css/                 # Стили
│   ├── js/                  # Основная логика
│   ├── items/               # Динамические страницы
│   └── index.html           # Главный HTML файл
├── package.json             # Зависимости и скрипты
└── README.md                # Документация
```

---

## 🧱 Технологический стек

| Уровень | Технологии |
|---------|------------|
| **Frontend** | Vanilla JS (ES6), HTML5, CSS3 |
| **Backend** | Node.js, Express |
| **База данных** | PostgreSQL 14+ |
| **Окружение** | dotenv, pg-pool |
| **Стиль** | Лёгкий адаптивный UI с модальными окнами, пагинацией и уведомлениями |

---

## 🧩 Особенности

- 🧩 Модульный дизайн с вдохновением от MVC  
- 🕵️ Валидация и поиск в реальном времени  
- 🗃️ Переиспользуемый API и динамическая маршрутизация  
- 🕒 Автоматические временные метки (`insertdate`, `updatedate`)  
- 🔄 Мягкое удаление и восстановление записей  
- ⚙️ Пагинация, сортировка и дебаунс поиска  
- 🧠 Система управления «Физлицами» и «Сотрудниками»  

---

## 🧠 Сущности базы данных

| Таблица | Описание |
|---------|----------|
| `cat1__nomenclature` | Базовый каталог (код, наименование, временные метки) |
| `cat2__individuals` | Физические лица (ИИН, поля имени, валидация) |
| `cat3__staffers` | Сотрудники, связанные с физлицами |

---

## 🖥️ Обзор страниц

| Страница | Описание |
|----------|----------|
| **Главная** | Основная таблица с CRUD операциями |
| **Физлица** | Управление записями физических лиц (ИИН, имя и др.) |
| **Сотрудники** | Управление сотрудниками, связанными с физлицами |

---

## 💡 Ключевые моменты

- Современная full-stack архитектура (Node.js + PostgreSQL)  
- Модульный frontend с динамическими маршрутами  
- Безопасный рендеринг и загрузчики  
- Мягкое удаление и восстановление в реальном времени  
- Встроенная валидация даты и кода  
- Чистый адаптивный дизайн

---
