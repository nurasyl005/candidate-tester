# 📋 Задания

Выберите любые задания и делайте в удобном темпе.
ИИ использовать можно.

## Правила создания таблиц

Все таблицы должны создаваться с префиксом (например `cat1__`, `cat2__`) и содержать обязательные поля:

```sql
<prefix>__id          -- SERIAL PRIMARY KEY
<prefix>__uuid        -- UUID DEFAULT uuid_generate_v4()
<prefix>__insertdate  -- TIMESTAMPTZ DEFAULT now()
<prefix>__updatedate  -- TIMESTAMPTZ DEFAULT now()
<prefix>__deleted     -- BOOL DEFAULT false
<prefix>__deletedate  -- TIMESTAMPTZ NULL
<prefix>__isfolder    -- BOOL DEFAULT false
<prefix>_<prefix>__folder -- UUID NULL (foreign key)
<prefix>__code        -- TEXT NULL
<prefix>__represent   -- TEXT NOT NULL
```

Далее добавляйте свои специфичные поля с тем же префиксом.

---

## 🔧 Фичи и доработки

### Задание 1: Валидация
Добавьте валидацию поля `code` - минимум 3 символа, только латинские буквы/цифры/дефис, проверка уникальности, показ ошибок в реальном времени.

### Задание 2: Работа с датами
Отобразите `insertdate` в таблице в удобном формате. При редактировании обновляйте `updatedate` по умолчанию на бэке.

### Задание 3: Поиск
Добавьте поиск по `code` и `represent`. Работа по мере ввода с debounce.

### Задание 4: Soft Delete
Реализуйте мягкое удаление через поле `deleted`. Удаленные записи не показывать. Переключатель "Показать удаленные". Возможность восстановления.

### Задание 5: Пагинация и сортировка
Пагинация с `page`, `limit`. Сортировка по колонкам.

### Задание 6: Улучшение UX/UI
Улучшите дизайн таблиц и форм для более выдержанного бизнес-варианта (меньше отступов, профессиональный вид). Переделайте модальное окно редактирования на более удобный вариант. Обязательно: показ загрузки при всех операциях и корректная обработка всех ошибок.

---

## 🏗️ Полноценное создание

### Задание 1: Система "Физические лица"

Создайте комплексную страницу для работы с физическими лицами и сотрудниками:

**1.1 Базовые таблицы:**
- `cat2__individuals` (ИИН, фамилия, имя, отчество, represent = ФИО)
- `cat3__staffers` (foreign key на individuals, табельный номер, represent = "Табельный: XXX - ФИО")

**1.2 Функционал:**
- Список всех физлиц с возможностью создания/редактирования/удаления
- Поиск по ИИН - показ полной информации + данные сотрудника (если есть)
- Создание записи сотрудника с выбором из списка физлиц

**1.3 UI/UX:**
- Удобный интерфейс с состояниями загрузки и обработкой ошибок
- Валидация ИИН (12 символов, уникальность) - обязательно
